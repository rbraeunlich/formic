language: scala
scala:
  - 2.11.8
jdk:
  - oraclejdk8
sudo: required
install:
  - nvm install 6.9.1
  - npm install jsdom
script:
  sbt 'set inScope(ThisScope in websockettestsJS)(postLinkJSEnv := new org.scalajs.jsenv.RetryingComJSEnv(PhantomJSEnv().value))' ++2.11.8 test